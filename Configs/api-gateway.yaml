apiVersion: v1
kind: Pod
metadata:
  name: api-gateway
spec:
	containers:
		-name: api-gateway
     image: api-gateway 
     ports:
     	- containerPort: 8080
     env:
	    - name: EUREKA_SERVER
	    value: service-discovery